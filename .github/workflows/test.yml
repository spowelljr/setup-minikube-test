name: CI
on:
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    name: job1
    steps:
    - name: start minikube
      id: minikube
      uses: spowelljr/setup-minikube@master
    - name: kubectl
      run: kubectl get pods -A
